using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Fireball : MonoBehaviour
{
        if (Input.GetMouseButtonDown(0))

      private Instantiate(fireballPrefab,
        attackPoint.
        position, attackPoint.
        rotation);

}
